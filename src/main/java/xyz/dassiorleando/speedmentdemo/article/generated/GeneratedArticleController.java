package xyz.dassiorleando.speedmentdemo.article.generated;

import com.speedment.common.annotation.GeneratedCode;
import com.speedment.common.json.Json;
import com.speedment.enterprise.plugins.json.JsonCollectors;
import com.speedment.enterprise.plugins.json.JsonComponent;
import com.speedment.enterprise.plugins.json.JsonEncoder;
import com.speedment.enterprise.plugins.spring.runtime.AbstractFilter;
import com.speedment.enterprise.plugins.spring.runtime.AbstractSort;
import com.speedment.enterprise.plugins.spring.runtime.ControllerUtil;
import com.speedment.runtime.field.Field;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;
import xyz.dassiorleando.speedmentdemo.article.Article;
import xyz.dassiorleando.speedmentdemo.article.ArticleManager;
import xyz.dassiorleando.speedmentdemo.article.generated.GeneratedArticle.Identifier;

import java.time.LocalDateTime;
import java.util.Comparator;
import java.util.EnumSet;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import java.util.function.Predicate;
import java.util.stream.Stream;
import javax.annotation.PostConstruct;

import static java.util.stream.Collectors.toList;

/**
 * The default REST controller logic for Article entities.
 * <p>
 * This file has been automatically generated by Speedment. Any changes made to
 * it will be overwritten.
 * 
 * @author Speedment
 */
@GeneratedCode("Speedment")
@CrossOrigin(origins = "*", maxAge = 3600)
public abstract class GeneratedArticleController {
    
    protected @Autowired JsonComponent jsonComponent;
    protected @Autowired ArticleManager manager;
    protected JsonEncoder<Article> encoder;
    
    @PostConstruct
    void createArticleEncoder() {
        encoder = jsonComponent.<Article>emptyEncoder()
            .put("id", Article.ID)
            .put("title", Article.TITLE)
            .put("content", Article.CONTENT)
            .put("publishedDate", Article.PUBLISHED_DATE)
            .build();
    }
    
    @GetMapping(path = "/articles", produces = "application/json")
    public String get(
            @RequestParam(name = "filter", defaultValue = "[]") String filters,
            @RequestParam(name = "sort", defaultValue = "[]") String sorters,
            @RequestParam(value = "start", defaultValue = "0") long start,
            @RequestParam(value = "limit", defaultValue = "25") long limit) {
        
        return getHelper(
            ControllerUtil.parseFilters(filters, ArticleFilter::new).collect(toList()),
            ControllerUtil.parseSorts(sorters, ArticleSort::new).collect(toList()),
            start,
            limit
        );
    }
    
    protected final Set<Identifier> parseColumns(String jsonColumnList) {
        try {
            @SuppressWarnings("unchecked")
            final List<String> parsed = (List<String>) Json.fromJson(jsonColumnList);
            final Set<GeneratedArticle.Identifier> result = EnumSet.noneOf(GeneratedArticle.Identifier.class);
            parsed.stream().map(this::parseColumn).forEach(result::add);
            return result;
        } catch (final ClassCastException ex) {
            throw new IllegalArgumentException("Error in parsed JSON.");
        }
    }
    
    protected final Identifier parseColumn(String jsonColumn) {
        switch (jsonColumn) {
            case "id":            return GeneratedArticle.Identifier.ID;
            case "title":         return GeneratedArticle.Identifier.TITLE;
            case "content":       return GeneratedArticle.Identifier.CONTENT;
            case "publishedDate": return GeneratedArticle.Identifier.PUBLISHED_DATE;
            default: throw new IllegalArgumentException(
                "Unknown column '" + jsonColumn + "'."
            );
        }
    }
    
    protected final Field<Article> fieldOf(Identifier columnId) {
        switch (columnId) {
            case ID:             return Article.ID;
            case TITLE:          return Article.TITLE;
            case CONTENT:        return Article.CONTENT;
            case PUBLISHED_DATE: return Article.PUBLISHED_DATE;
            default: throw new IllegalArgumentException(
                "Unknown column '" + columnId + "'."
            );
        }
    }
    
    protected String getHelper(
            List<Predicate<Article>> predicates,
            List<Comparator<Article>> sorters,
            long start,
            long limit) {
        Stream<Article> stream = manager.stream();
        
        for (final Predicate<Article> predicate : predicates) {
            stream = stream.filter(predicate);
        }
        
        if (!sorters.isEmpty()) {
            final Optional<Comparator<Article>> comparator = sorters.stream()
                .reduce(Comparator::thenComparing);
            
            stream = stream.sorted(comparator.get());
        }
        
        return stream
            .skip(start)
            .limit(limit)
            .collect(JsonCollectors.toList(encoder));
    }
    
    /**
     * How to filter the results from the controller. This class is designed to
     * be deserialized automatically from JSON.
     */
    public final static class ArticleFilter extends AbstractFilter<Article> {
        
        public ArticleFilter(
                String operator,
                String property,
                String value) {
            super(operator, property, value);
        }
        
        @Override
        public Predicate<Article> toPredicate() {
            switch (property()) {
                case "id" : {
                    final int v = Integer.parseInt(value());
                    switch (operator()) {
                        case "eq"   : return Article.ID.equal(v);
                        case "ne"   : return Article.ID.notEqual(v);
                        case "lt"   : return Article.ID.lessThan(v);
                        case "le"   : return Article.ID.lessOrEqual(v);
                        case "gt"   : return Article.ID.greaterThan(v);
                        case "ge"   : return Article.ID.greaterOrEqual(v);
                        case "like" : // Fallthrough
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Article.id."
                        );
                    }
                }
                case "title" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return Article.TITLE.equal(v);
                        case "ne"   : return Article.TITLE.notEqual(v);
                        case "lt"   : return Article.TITLE.lessThan(v);
                        case "le"   : return Article.TITLE.lessOrEqual(v);
                        case "gt"   : return Article.TITLE.greaterThan(v);
                        case "ge"   : return Article.TITLE.greaterOrEqual(v);
                        case "like" : return Article.TITLE.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Article.title."
                        );
                    }
                }
                case "content" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return Article.CONTENT.equal(v);
                        case "ne"   : return Article.CONTENT.notEqual(v);
                        case "lt"   : return Article.CONTENT.lessThan(v);
                        case "le"   : return Article.CONTENT.lessOrEqual(v);
                        case "gt"   : return Article.CONTENT.greaterThan(v);
                        case "ge"   : return Article.CONTENT.greaterOrEqual(v);
                        case "like" : return Article.CONTENT.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Article.content."
                        );
                    }
                }
                case "publishedDate" : {
                    final LocalDateTime v = LocalDateTime.parse(value());
                    switch (operator()) {
                        case "eq"   : return Article.PUBLISHED_DATE.equal(v);
                        case "ne"   : return Article.PUBLISHED_DATE.notEqual(v);
                        case "lt"   : return Article.PUBLISHED_DATE.lessThan(v);
                        case "le"   : return Article.PUBLISHED_DATE.lessOrEqual(v);
                        case "gt"   : return Article.PUBLISHED_DATE.greaterThan(v);
                        case "ge"   : return Article.PUBLISHED_DATE.greaterOrEqual(v);
                        case "like" : // Fallthrough
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Article.publishedDate."
                        );
                    }
                }
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid Article property."
                );
            }
        }
    }
    
    /**
     * How to sort the results from the controller. This class is designed to be
     * deserialized automatically from JSON.
     */
    public final static class ArticleSort extends AbstractSort<Article> {
        
        public ArticleSort(String property, String direction) {
            super(property, direction);
        }
        
        @Override
        public Comparator<Article> toComparator() {
            final Comparator<Article> comparator;
            switch (property()) {
                case "id"            : comparator = Article.ID.comparator();             break;
                case "title"         : comparator = Article.TITLE.comparator();          break;
                case "content"       : comparator = Article.CONTENT.comparator();        break;
                case "publishedDate" : comparator = Article.PUBLISHED_DATE.comparator(); break;
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid Article property."
                );
            }
            
            switch (direction()) {
                case "ASC"  : return comparator;
                case "DESC" : return comparator.reversed();
                default : throw new IllegalArgumentException(
                    "'" + direction() + "' is not a valid sort direction. " +
                    "Use either 'ASC' or 'DESC', or leave out."
                );
            }
        }
    }
}